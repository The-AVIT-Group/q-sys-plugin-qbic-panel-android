<?xml version="1.0" encoding="utf-8"?>
<!-- Declares the device admin capabilities used by this app.
     Only FORCE_LOCK is needed — to lock the screen on the "screen off" command. -->
<device-admin>
  <uses-policies>
    <force-lock />
  </uses-policies>
</device-admin>
